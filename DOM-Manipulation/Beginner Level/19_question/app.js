// Write a JavaScript function to get the sibling elements of a node. (Concept: DOM Traversal)

const ul = document.querySelector('ul');
const liList = document.querySelector('li');

const getSiblings = function(node){
    if(!node) return `Please Enter `;      
}