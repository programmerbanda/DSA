// Write a JavaScript function to use array methods like reduce() for aggregation. (Concept:
// Array Methods)


const arr = [1, 2, 3, 4, 5, 6, 7, 8];
const total = arr.reduce((acc, to) => acc + to, 0);
console.log(total) 